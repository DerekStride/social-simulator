<%= form_for(@social_user) do |f| %>
  <% if @social_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@social_user.errors.count, "error") %> prohibited this social_user from being saved:</h2>

      <ul>
      <% @social_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
